# Assemble the three-row controlled-experiments figure:
#   (a) full-width top
#   (b) full-width middle
#   (c) full-width bottom
# so (c) no longer overlaps the third (b) subplot.

docs <- "/Users/chegu121/Documents/Phd-cici/r_package/mcmcConvergenceGraph/docs"
out  <- file.path(docs, "fig_controlled_experiments.tex")

panel_a <- readLines(file.path(docs, "fig_experiment1_panel_a.tex"))
panel_b <- readLines(file.path(docs, "fig_experiment2_panel_b.tex"))
panel_a <- sub("\\\\def\\\\py\\{3\\.66\\}", "\\\\def\\\\py{7.48}", panel_a)

header <- c(
  "% Add to the preamble:",
  "% \\usepackage{graphicx}",
  "% \\usepackage{float}",
  "% \\usepackage{tikz}",
  "% \\usetikzlibrary{arrows.meta,calc,positioning}",
  "",
  "\\begin{figure}[H]",
  "\\centering",
  "",
  "% Three stacked rows: (a) targets, (b) two-line regressions, (c) PK schematic.",
  "\\makebox[\\textwidth][c]{%",
  "\\resizebox{\\textwidth}{!}{%",
  "\\begin{tikzpicture}[",
  "  font=\\small,",
  "  axis/.style={",
  "    -{Latex[length=1.5mm]},",
  "    line width=0.45pt",
  "  },",
  "  frame/.style={",
  "    draw=black,",
  "    line width=0.45pt",
  "  },",
  "  drawpoint/.style={",
  "    circle,",
  "    fill=gray!75,",
  "    draw=none,",
  "    inner sep=0pt,",
  "    minimum size=0.75mm",
  "  },",
  "  mixtureA/.style={",
  "    black,",
  "    line width=0.8pt",
  "  },",
  "  mixtureB/.style={",
  "    black,",
  "    dashed,",
  "    line width=0.8pt",
  "  },",
  " compartment/.style={",
  "  draw=black,",
  "  rounded corners=1.3mm,",
  "  align=center,",
  "  minimum width=1.35cm,",
  "  minimum height=0.72cm,",
  "  inner sep=0.8mm",
  "},",
  "  pkArrow/.style={",
  "    -{Latex[length=1.6mm]},",
  "    line width=0.55pt",
  "  }",
  "]",
  "",
  "% =========================================================",
  "% Overall figure dimensions: three full-width rows",
  "% =========================================================",
  "\\def\\W{13.2}",
  "\\def\\H{10.55}",
  "\\def\\rowab{7.10}",
  "\\def\\rowbc{3.50}",
  "",
  "% Outer frame",
  "\\draw[line width=0.55pt]",
  "  (0,0) rectangle (\\W,\\H);",
  "",
  "% Horizontal divisions between (a)/(b) and (b)/(c)",
  "\\draw[line width=0.55pt]",
  "  (0,\\rowab) -- (\\W,\\rowab);",
  "\\draw[line width=0.55pt]",
  "  (0,\\rowbc) -- (\\W,\\rowbc);",
  "",
  "% =========================================================",
  "% Panel labels",
  "% =========================================================",
  "\\node[",
  "  anchor=north west,",
  "  font=\\bfseries",
  "] at (0.15,\\H-0.15)",
  "{",
  "  (a)",
  "};",
  "",
  "\\node[",
  "  anchor=north west,",
  "  font=\\bfseries",
  "] at (0.15,\\rowab-0.15)",
  "{",
  "  (b)",
  "};",
  "",
  "\\node[",
  "  anchor=north west,",
  "  font=\\bfseries",
  "] at (0.15,\\rowbc-0.15)",
  "{",
  "  (c)",
  "};",
  ""
)

panel_c <- c(
  "",
  "% =========================================================",
  "% PANEL (c): three-compartment pharmacokinetic schematic",
  "% =========================================================",
  "",
  "\\node[",
  "  compartment",
  "] (vp2) at (3.55,1.65)",
  "{",
  "  $V_{P_2}$",
  "};",
  "",
  "\\node[",
  "  compartment",
  "] (vc) at (6.60,1.65)",
  "{",
  "  $V_C$",
  "};",
  "",
  "\\node[",
  "  compartment",
  "] (vp1) at (9.65,1.65)",
  "{",
  "  $V_{P_1}$",
  "};",
  "",
  "",
  "% Input rate",
  "\\draw[pkArrow]",
  "  ($(vc.north)+(0,0.62)$)",
  "  --",
  "  (vc.north);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=south",
  "] at ($(vc.north)+(0,0.62)$)",
  "{",
  "  $R_{\\mathrm{in}}$",
  "};",
  "",
  "",
  "% Central to peripheral compartment 1",
  "\\draw[pkArrow]",
  "  ($(vc.east)+(0,0.18)$)",
  "  --",
  "  ($(vp1.west)+(0,0.18)$);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=south",
  "] at ($(vc.east)!0.5!(vp1.west)+(0,0.18)$)",
  "{",
  "  $k_{12}$",
  "};",
  "",
  "",
  "% Peripheral compartment 1 to central",
  "\\draw[pkArrow]",
  "  ($(vp1.west)+(0,-0.18)$)",
  "  --",
  "  ($(vc.east)+(0,-0.18)$);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=north",
  "] at ($(vc.east)!0.5!(vp1.west)+(0,-0.18)$)",
  "{",
  "  $k_{21}$",
  "};",
  "",
  "",
  "% Central to peripheral compartment 2",
  "\\draw[pkArrow]",
  "  ($(vc.west)+(0,0.18)$)",
  "  --",
  "  ($(vp2.east)+(0,0.18)$);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=south",
  "] at ($(vp2.east)!0.5!(vc.west)+(0,0.18)$)",
  "{",
  "  $k_{13}$",
  "};",
  "",
  "",
  "% Peripheral compartment 2 to central",
  "\\draw[pkArrow]",
  "  ($(vp2.east)+(0,-0.18)$)",
  "  --",
  "  ($(vc.west)+(0,-0.18)$);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=north",
  "] at ($(vp2.east)!0.5!(vc.west)+(0,-0.18)$)",
  "{",
  "  $k_{31}$",
  "};",
  "",
  "",
  "% Elimination",
  "\\draw[pkArrow]",
  "  (vc.south)",
  "  --",
  "  ($(vc.south)+(0,-0.65)$);",
  "",
  "\\node[",
  "  font=\\scriptsize,",
  "  anchor=west",
  "] at ($(vc.south)+(0.06,-0.39)$)",
  "{",
  "  $k_{10}$",
  "};"
)

footer <- c(
  "",
  "\\end{tikzpicture}%",
  "}%",
  "}",
  "",
  "\\vspace{1mm}",
  "",
  "\\caption{",
  "\\textbf{Test problems used in the three experimental settings.}",
  "The three panels correspond to the three settings of Section~\\ref{sec:experimental-setup}, arranged from prescribed target distributions in~(a) to a real systems-biology model in~(c).",
  "%",
  "(a)~\\emph{Four prescribed target distributions.} Left to right: a uniform distribution on a two-dimensional box (\\emph{Uniform}); a single Gaussian ball (\\emph{One cluster}); a noisy two-dimensional line of the form $y \\approx a x + b + \\varepsilon$ (\\emph{Line with noise}); and a three-component Gaussian mixture with well-separated centres (\\emph{Three clusters}). Each panel shows a representative scatter of two-dimensional draws from the corresponding target distribution.",
  "%",
  "(b)~\\emph{Three variants of two Bayesian linear regressions on two-line data.} Solid and dashed lines depict posterior medians of the two data-generating regression lines $y = a_k x + b_k$ ($k = 1, 2$), from each of which half of the observations are drawn. \\emph{Different intercepts} imposes a shared slope ($a_1 = a_2$, $b_1 \\neq b_2$); \\emph{Different slopes} imposes a shared intercept ($a_1 \\neq a_2$, $b_1 = b_2$); and \\emph{Different slopes and intercepts} lets both vary freely ($a_1 \\neq a_2$, $b_1 \\neq b_2$). In each variant the fitted model treats the per-observation assignment to one of the two lines as unknown, so that the posterior over the component-specific coefficients that differ between the two lines is bimodal, reflecting the two-line structure of the data.",
  "%",
  "(c)~\\emph{Three-compartment pharmacokinetic model for remifentanil concentration data.} Remifentanil is infused into the central compartment~$V_C$ at rate $R_{\\mathrm{in}}$ and eliminated from $V_C$ at rate $k_{10}$. Drug transfers between $V_C$ and the peripheral compartments at rates $k_{12},\\,k_{21}$ (forward and backward exchange with~$V_{P_1}$) and $k_{13},\\,k_{31}$ (forward and backward exchange with~$V_{P_2}$). The two peripheral pathways are exchangeable under the swap $(k_{12},\\,k_{21}) \\leftrightarrow (k_{13},\\,k_{31})$, so the posterior over the peripheral rate constants admits label-switched multimodal regions.",
  "}",
  "\\label{fig:controlled-experiments}",
  "\\end{figure}"
)

tex <- c(header, "", panel_a, "", panel_b, panel_c, footer)
writeLines(tex, out, useBytes = TRUE)
cat("Wrote", out, "lines", length(tex), "\n")
