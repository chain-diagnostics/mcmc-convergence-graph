% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/14_mcmc_graph_summary.R
\name{mcmc_graph_summary}
\alias{mcmc_graph_summary}
\title{Summarize pairwise R-hat graph structure across parameters}
\usage{
mcmc_graph_summary(
  draws,
  parameters = NULL,
  rho = 1.05,
  save_csv = FALSE,
  output_dir = NULL,
  pairwise_display_n = 10
)
}
\arguments{
\item{draws}{A three-dimensional array of posterior draws with dimensions
iterations by chains by parameters, or an rstan \code{stanfit} object.}

\item{parameters}{Optional character vector of parameter names. If \code{NULL},
all parameters in \code{draws} are used.}

\item{rho}{Numeric threshold used to decide whether two chains are connected.
For each parameter, an edge is added when the pairwise R-hat value is less
than or equal to \code{rho}.}

\item{save_csv}{Logical. If \code{TRUE}, numerical outputs are saved as CSV files.
The default is \code{FALSE}.}

\item{output_dir}{Optional character string giving the directory where CSV
files should be saved. This must be provided when \code{save_csv = TRUE}.}

\item{pairwise_display_n}{Integer giving the number of largest pairwise R-hat
values to include in \code{pairwise_values_display}. The default is 10. Values
larger than 20 are capped at 20.}
}
\value{
A list with eight elements:
\describe{
\item{summary}{A data frame with one row per parameter, giving the number of multi-chain clusters (\code{n_clusters}), the number of isolated chains (\code{n_isolated}), the cluster composition (\code{clusters}), the isolated chain identifiers (\code{isolated_chains}), and the largest pairwise R-hat over within-cluster pairs and pairs involving isolated chains (\code{pairwise_rhat_value}; \code{NA} when fewer than two chains are available).}
\item{pairwise_values_display}{A shortened data frame containing the largest pairwise R-hat values for display.}
\item{pairwise_values}{A full data frame containing one pairwise R-hat value for each parameter and chain pair.}
\item{clusters}{A named list of detailed cluster results for each parameter.}
\item{graphs}{A named list of parameter-specific \code{igraph} objects.}
\item{combined_graph}{The union combined \code{igraph} object across parameters (\code{G_union}). An edge is present whenever the chain pair is connected in at least one parameter-specific graph. Pass to \code{\link[=plot_mcmc_graph]{plot_mcmc_graph()}} to visualise \code{G_union}.}
\item{combined_graph_intersection}{The intersection combined \code{igraph} object across parameters (\code{G_intersection}). An edge is present only when the chain pair is connected in every parameter-specific graph. This is the graph whose connected components carry the multivariate mode count.}
\item{combined_graph_difference}{The set difference \code{G_union} minus \code{G_intersection}. Edges are chain pairs that agree on some monitored dimensions and disagree on others.}
\item{intersection_summary}{A one-row data frame giving the multi-chain clusters and isolated chains of \code{G_intersection}. This is the multivariate replacement for the per-dimension \code{summary} when counting modes.}
\item{intersection_clusters}{The detailed cluster list from \code{\link[=mcmc_graph_components]{mcmc_graph_components()}} applied to \code{G_intersection}.}
\item{rhat_matrices}{A named list of pairwise R-hat matrices.}
\item{rho}{The threshold used to define graph edges.}
}
}
\description{
Computes parameter-specific pairwise R-hat matrices, converts each matrix into
a graph, and summarizes the graph structure for each parameter. The summary
reports the number of multi-chain clusters, the number of isolated chains,
the cluster composition, and the pairwise R-hat value for each parameter.
}
\details{
The reported \code{pairwise_rhat_value} is the largest pairwise R-hat over
(i) pairs that lie in the same multi-chain cluster of the parameter-specific
graph at threshold \code{rho}, and (ii) pairs that involve at least one isolated
chain. Pairs between two different multi-chain clusters are excluded, so a
cleanly separated multimodal posterior in which each mode is internally
coherent still yields a small \code{pairwise_rhat_value}, even though the
classical R-hat across all chains would be large. Isolated chains, by
contrast, are treated as a mixing problem and are included in the maximum,
so \code{pairwise_rhat_value} rises when \code{n_isolated > 0}. When a parameter has
fewer than two chains, \code{pairwise_rhat_value} is \code{NA}.

The function also returns a full table of pairwise R-hat values for every
parameter and chain pair. For convenient display, it additionally returns a
shortened table containing the largest pairwise R-hat values. By default, the
display table contains the top 10 values, and the maximum allowed display
size is 20.
}
\examples{
set.seed(1)

draws <- array(
  rnorm(100 * 4 * 2),
  dim = c(100, 4, 2)
)

dimnames(draws) <- list(
  NULL,
  paste0("chain", 1:4),
  c("alpha", "beta")
)

result <- mcmc_graph_summary(
  draws = draws,
  parameters = c("alpha", "beta"),
  rho = 1.05,
  save_csv = FALSE
)

result$summary
result$pairwise_values_display
}
