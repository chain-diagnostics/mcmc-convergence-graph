% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/12_plot_mcmc_graph.R
\name{plot_mcmc_graph}
\alias{plot_mcmc_graph}
\title{Plot a combined pairwise R-hat graph}
\usage{
plot_mcmc_graph(
  graph,
  show_edge_labels = FALSE,
  show_legend = TRUE,
  layout_matrix = NULL,
  layout_type = c("grid", "fr", "kk", "nicely", "circle", "random", "tree", "drl"),
  main = "Combined pairwise R-hat graph",
  vertex_size = 20,
  vertex_label_cex = 1.8,
  edge_label_cex = 0.8,
  edge_curved = 0.2,
  legend_position = "top",
  legend_cex = 0.8,
  show_node_note = FALSE
)
}
\arguments{
\item{graph}{An \code{igraph} object created by \code{\link[=mcmc_graph_multi]{mcmc_graph_multi()}}.
The default \code{mcmcgraph()} figure passes the union graph so that
shared (black) edges are \code{G_intersection} and coloured edges are the
additional edges \code{G_union} minus \code{G_intersection}.}

\item{show_edge_labels}{Logical. If \code{TRUE}, edge labels show the parameters
supporting each edge. The default is \code{FALSE}.}

\item{show_legend}{Logical. If \code{TRUE}, a legend showing parameter colors is
added to the plot.}

\item{layout_matrix}{Optional numeric matrix giving node positions. If \code{NULL},
the layout is computed automatically using \code{layout_type}.}

\item{layout_type}{Character string specifying the automatic layout to use
when \code{layout_matrix} is \code{NULL}. Options are \code{"grid"}, \code{"fr"}, \code{"kk"},
\code{"nicely"}, \code{"circle"}, \code{"random"}, \code{"tree"}, and \code{"drl"}.}

\item{main}{Character string giving the plot title.}

\item{vertex_size}{Numeric value controlling node size.}

\item{vertex_label_cex}{Numeric value controlling chain-label size.}

\item{edge_label_cex}{Numeric value controlling edge-label size.}

\item{edge_curved}{Numeric value controlling edge curvature. The default is
\code{0.2} (slightly bowed edges). Use \code{0} for straight chords.}

\item{legend_position}{Character string giving the legend position.}

\item{legend_cex}{Numeric value controlling legend text size.}

\item{show_node_note}{Logical. If \code{TRUE}, adds a note saying that node labels
represent chain indices.}
}
\value{
Invisibly returns the layout matrix used for the plot.
}
\description{
Plots a combined pairwise R-hat graph created by
\code{\link[=mcmc_graph_multi]{mcmc_graph_multi()}}. Chains are shown as nodes. On a
multivariate graph, solid black edges are \code{G_intersection} (agree on
every dimension) and dashed grey edges are \code{G_union} minus
\code{G_intersection} (agree on some dimensions only), matching Fig. 2(c).
}
\examples{
set.seed(1)

draws <- array(
  rnorm(100 * 4 * 2),
  dim = c(100, 4, 2)
)

dimnames(draws) <- list(
  NULL,
  paste0("chain", 1:4),
  c("alpha", "beta")
)

graph <- mcmc_graph_multi(
  draws = draws,
  parameters = c("alpha", "beta"),
  rho = 1.05
)

plot_mcmc_graph(
  graph,
  layout_type = "grid"
)
}
