% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/13_plot_mcmc_graph.R
\name{plot_mcmc_graph}
\alias{plot_mcmc_graph}
\title{Plot a combined pairwise R-hat graph}
\usage{
plot_mcmc_graph(
  graph,
  graph_intersection = NULL,
  show_edge_labels = FALSE,
  show_legend = TRUE,
  layout_matrix = NULL,
  layout_type = c("circle", "grid", "fr", "kk", "nicely", "random", "tree", "drl"),
  main = "",
  vertex_size = 20,
  vertex_label_cex = 1.8,
  edge_label_cex = 0.8,
  edge_curved = 0.1,
  legend_position = "top",
  legend_cex = 0.8,
  mar = c(0.4, 0.4, 0.4, 0.4)
)
}
\arguments{
\item{graph}{An \code{igraph} object, usually \code{result$combined_graph} from
\code{\link[=mcmcgraph]{mcmcgraph()}}.
The default \code{mcmcgraph()} figure passes the union graph \eqn{G_{\cup}}
so that shared (black) edges are \eqn{G_{\cap}} and dashed grey edges are
\eqn{G_{\cup} \setminus G_{\cap}}.}

\item{graph_intersection}{Optional intersection graph \eqn{G_{\cap}}. When
\code{layout_matrix} is \code{NULL}, nodes from the same \eqn{G_{\cap}} component
sit together on the circle.}

\item{show_edge_labels}{Logical. If \code{TRUE}, edge labels show the parameters
supporting each edge. The default is \code{FALSE}.}

\item{show_legend}{Logical. If \code{TRUE} (default), a legend is added.}

\item{layout_matrix}{Optional numeric matrix giving node positions. If \code{NULL},
the layout is computed from \code{layout_type}.}

\item{layout_type}{Character string. The default is \code{"circle"}. Other
options are \code{"grid"}, \code{"fr"}, \code{"kk"}, \code{"nicely"}, \code{"random"},
\code{"tree"}, and \code{"drl"}.}

\item{main}{Character string giving the plot title. The default is empty.}

\item{vertex_size}{Numeric value controlling node size.}

\item{vertex_label_cex}{Numeric value controlling chain-label size.}

\item{edge_label_cex}{Numeric value controlling edge-label size.}

\item{edge_curved}{Numeric curvature passed to igraph. The default is \code{0}
(straight chords).}

\item{legend_position}{Character string giving the legend position.}

\item{legend_cex}{Numeric value controlling legend text size.}

\item{mar}{Numeric vector of four margins passed to \code{\link[graphics:par]{graphics::par()}},
in the order bottom, left, top, right.}
}
\value{
Invisibly returns the layout matrix used for the plot.
}
\description{
Plots a combined pairwise R-hat graph. Chains are shown as nodes. On a
multivariate graph, solid black edges are the intersection graph
\eqn{G_{\cap}} (agree on every dimension) and dashed grey edges are
the set difference \eqn{G_{\cup} \setminus G_{\cap}}.
}
\examples{
set.seed(1)

draws <- array(
  rnorm(100 * 4 * 2),
  dim = c(100, 4, 2)
)

dimnames(draws) <- list(
  NULL,
  paste0("chain", 1:4),
  c("alpha", "beta")
)

result <- mcmcgraph(
  draws = draws,
  parameters = c("alpha", "beta"),
  rho = 1.05,
  plot = FALSE
)

plot_mcmc_graph(
  result$combined_graph,
  graph_intersection = result$combined_graph_intersection
)
}
