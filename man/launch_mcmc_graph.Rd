% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_launch_mcmc_graph.R
\name{launch_mcmc_graph}
\alias{launch_mcmc_graph}
\alias{mcmcgraph}
\title{Run the MCMC convergence graph workflow}
\usage{
launch_mcmc_graph(
  draws,
  parameters = NULL,
  rho = 1.05,
  plot = TRUE,
  save_csv = FALSE,
  save_plot = FALSE,
  output_dir = NULL,
  pairwise_display_n = 10,
  ...
)

mcmcgraph(
  draws,
  parameters = NULL,
  rho = 1.05,
  plot = TRUE,
  save_csv = FALSE,
  save_plot = FALSE,
  output_dir = NULL,
  pairwise_display_n = 10,
  ...
)
}
\arguments{
\item{draws}{A three-dimensional array of posterior draws with dimensions
iterations by chains by parameters, or an rstan \code{stanfit} object.}

\item{parameters}{Optional character vector of parameter names. If \code{NULL},
all parameters in \code{draws} are used.}

\item{rho}{Numeric threshold used to decide whether two chains are connected.}

\item{plot}{Logical. If \code{TRUE} (default), one combined graph is drawn:
node positions come from \code{G_intersection}, black solid edges are
\code{G_intersection}, and coloured dashed edges are the additional edges
\code{G_union} minus \code{G_intersection}.}

\item{save_csv}{Logical. If \code{TRUE}, numerical outputs are saved as CSV files.
The default is \code{FALSE}.}

\item{save_plot}{Logical. If \code{TRUE}, the combined intersection-plus-
additional-edge graph is written to \code{combined_graph.pdf} inside
\code{output_dir} (8 x 6 inches). Requires \code{output_dir}. The default is \code{FALSE}.}

\item{output_dir}{Optional character string giving the directory where CSV
files and/or the PDF plot should be saved. Required when \code{save_csv = TRUE}
or \code{save_plot = TRUE}.}

\item{pairwise_display_n}{Integer giving the number of largest pairwise
R-hat values to include in \code{pairwise_values_display}. The default is 10.}

\item{...}{Additional arguments passed to \code{\link[=plot_mcmc_graph]{plot_mcmc_graph()}}.}
}
\value{
Invisibly returns the list produced by
\code{\link[=mcmc_graph_summary]{mcmc_graph_summary()}}. The fields are \code{summary},
\code{pairwise_values_display}, \code{pairwise_values}, \code{clusters}, \code{graphs},
\code{combined_graph} (the union graph \code{G_union}),
\code{combined_graph_intersection} (the intersection graph \code{G_intersection}),
\code{combined_graph_difference} (\code{G_union} minus \code{G_intersection}),
\code{intersection_summary}, \code{rhat_matrices}, and \code{rho}.
}
\description{
Convenience wrapper around the MCMC convergence graph pipeline. Computes
parameter-specific pairwise R-hat matrices, per-parameter graphs, component
summaries, the combined graphs \code{G_union}, \code{G_intersection}, and
\code{G_union} minus \code{G_intersection}, and (by default) draws one graph:
the intersection layout with additional \code{G_union} minus \code{G_intersection}
edges overlaid. Optionally also writes the plot to a PDF file.
}
\examples{
set.seed(1)

draws <- array(
  rnorm(100 * 4 * 2),
  dim = c(100, 4, 2)
)

dimnames(draws) <- list(
  NULL,
  paste0("chain", 1:4),
  c("alpha", "beta")
)

result <- mcmcgraph(
  draws = draws,
  parameters = c("alpha", "beta"),
  rho = 1.05,
  plot = FALSE
)

result$summary
}
