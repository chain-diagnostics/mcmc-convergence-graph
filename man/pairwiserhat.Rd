% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/16_launch_pairwiserhat.R
\name{pairwiserhat}
\alias{pairwiserhat}
\title{Run the full pairwise R-hat workflow}
\usage{
pairwiserhat(
  draws,
  parameters = NULL,
  rho = 1.01,
  plot = TRUE,
  save_csv = FALSE,
  save_plot = FALSE,
  output_dir = NULL,
  pairwise_display_n = 10,
  posterior_draws_parameters = NULL,
  ...
)
}
\arguments{
\item{draws}{A three-dimensional array of posterior draws with dimensions
iterations by chains by parameters, or an rstan \code{stanfit} object.}

\item{parameters}{Optional character vector of parameter names. If \code{NULL},
all parameters in \code{draws} are used.}

\item{rho}{Numeric threshold used to decide whether two chains are connected.}

\item{plot}{Logical. If \code{TRUE} (default), the intersection combined graph
\code{G_intersection} is drawn using \code{\link[=plot_pairwise_rhat_combined_graph]{plot_pairwise_rhat_combined_graph()}} on
the current graphics device, followed by the posterior-draws scatter from
\code{\link[=plot_posterior_draws]{plot_posterior_draws()}}. Pass \code{result$combined_graph} to
\code{\link[=plot_pairwise_rhat_combined_graph]{plot_pairwise_rhat_combined_graph()}} manually to plot the union graph
\code{G_union} instead.}

\item{save_csv}{Logical. If \code{TRUE}, numerical outputs are saved as CSV files.
The default is \code{FALSE}.}

\item{save_plot}{Logical. If \code{TRUE}, the intersection combined graph
\code{G_intersection} is written to \code{combined_graph.pdf} and the posterior-draws
scatter is written to \code{posterior_draws.pdf} inside \code{output_dir}
(both 8 x 6 inches). Requires \code{output_dir}. The default is \code{FALSE}.}

\item{output_dir}{Optional character string giving the directory where CSV
files and/or the PDF plots should be saved. Required when \code{save_csv = TRUE}
or \code{save_plot = TRUE}.}

\item{pairwise_display_n}{Integer giving the number of largest pairwise
R-hat values to include in \code{pairwise_values_display}. The default is 10.}

\item{posterior_draws_parameters}{Optional character vector of length two
giving the parameters used for the posterior-draws scatter. If \code{NULL}
(the default), the first two monitored parameters are used. The
posterior-draws plot is skipped, with a warning, if \code{ggplot2} is not
installed or fewer than two monitored parameters are available.}
}
\value{
Invisibly returns the list produced by
\code{\link[=pairwise_rhat_parameter_summary]{pairwise_rhat_parameter_summary()}}, augmented with
\code{posterior_draws_plot} (a \code{ggplot} object, or \code{NULL} when the
posterior-draws plot was skipped). The other fields are \code{summary},
\code{pairwise_values_display}, \code{pairwise_values}, \code{clusters}, \code{graphs},
\code{combined_graph} (the union graph \code{G_union}),
\code{combined_graph_intersection} (the intersection graph \code{G_intersection}),
\code{rhat_matrices}, and \code{rho}.
}
\description{
Convenience wrapper around the pairwise R-hat pipeline. Computes
parameter-specific pairwise R-hat matrices, per-parameter graphs, cluster
summaries, both across-parameter combined graphs (union \code{G_union} and
intersection \code{G_intersection}), and (by default) draws the intersection
combined graph \code{G_intersection} and a 2D posterior-draws scatter for the
first two monitored parameters. Optionally also writes both plots to PDF
files.
}
\examples{
set.seed(1)

draws <- array(
  rnorm(100 * 4 * 2),
  dim = c(100, 4, 2)
)

dimnames(draws) <- list(
  NULL,
  paste0("chain", 1:4),
  c("alpha", "beta")
)

result <- pairwiserhat(
  draws = draws,
  parameters = c("alpha", "beta"),
  rho = 1.015,
  plot = FALSE
)

result$summary
}
